import * as React from "react";
import { registerWidget, registerLink, registerUI, IContextProvider, } from './uxp';
import { TitleBar, FilterPanel, WidgetWrapper } from "uxp/components";
import './styles.scss';

interface IWidgetProps {
    uxpContext?: IContextProvider,
    instanceId?: string
}

const __WIDGETNAME__Widget: React.FunctionComponent<IWidgetProps> = (props) => {
    return (
        <WidgetWrapper>
            <TitleBar title='__WIDGETNAME__'>
                <FilterPanel>
                </FilterPanel>
            </TitleBar>
        </WidgetWrapper>
    )
};

/**
 * Register as a Widget
 */
registerWidget({
    id: "__WIDGET__",
    widget: __WIDGETNAME__Widget,
    configs: {
        layout: {
            // w: 12,
            // h: 12,
            // minH: 12,
            // minW: 12
        }
    }
});

/**
 * Register as a Sidebar Link
 */
/*
registerLink({
    id: "__WIDGET__",
    label: "__WIDGETNAME__",
    // click: () => alert("Hello"),
    component: __WIDGETNAME__Widget
});
*/

/**
 * Register as a UI
 */

 /*
registerUI({
    id:"__WIDGET__",
    component: __WIDGETNAME__Widget
});
*/